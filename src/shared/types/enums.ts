export enum LoadingStatus {
  Idle = 'idle',
  Pending = 'pending',
  Succeeded = 'succeeded',
  Failed = 'failed',
}

export enum AllLeaguesTableColumns {
  LeagueName = 'league_name',
  LeagueID = 'table_id',
  CretedAt = 'created_at',
}
